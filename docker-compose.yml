version: "3.8"
services:
  backend:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - 8000:8080
    networks:
      - net
    environment:
      APP_ENVIRONMENT: production

networks:
  net: {}
